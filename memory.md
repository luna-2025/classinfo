# 작업 메모리

## 2026-08-12 진행 내용
- 작업 이름을 `수업 기록 관리 사이트`로 변경했다.
- 사용자가 준 대시보드 HTML을 기반으로 `index.html`을 만들었다.
- 예시로 들어 있던 오늘 수업 3개를 삭제하고 빈 상태 메시지로 변경했다.
- 상단 제목을 `수업 기록`으로 변경하고 날짜 선택 달력을 추가했다.
- 섹션 제목을 `수업 내용`, 버튼을 `+ 수업 추가`로 변경했다.
- 과정/반 예시 선택 목록을 제거하고 직접 입력 방식으로 변경했다.
- 수업 기록하기 모달을 추가했다.
  - 좋았던 점, 아쉬웠던 점, 다음 수업 개선 사항
  - 학생별 피드백 추가 기능
- 반복 수업 기능을 추가했다.
  - 수업 시작일, 수업 시간, 매주 같은 요일 반복 체크, 반복 종료일
  - 선택 날짜가 반복 요일과 기간에 맞으면 수업이 표시된다.
- 수업 카드에 삭제 기능을 추가했다.
  - 현재 반복 수업 삭제는 전체 반복 일정을 삭제한다.
- localStorage 저장을 추가했다.
  - 수업 일정: `classRecords`
  - 수업 기록: `lessonRecords`
  - 새로고침 뒤에도 같은 브라우저/기기에서는 유지된다.

## 사용자 선호
- 수정 후 자동으로 미리보기를 열어주기
- 어려운 기술 설명보다 바로 작동하는 화면을 선호
- 로그인은 원하지 않지만 PC/휴대폰 데이터 동기화는 추후 원함

## 알려진 한계
- localStorage는 휴대폰과 PC 사이에서 동기화되지 않는다.
- 엑셀 학생 명단 가져오기는 아직 구현하지 않았다.
- 실제 학생 명단과 과정별 소속은 데이터 구조에 아직 연결되지 않았다.
- 날짜별 수업 기록 화면과 학생 관리 메뉴는 아직 완성되지 않았다.
- Supabase 프로젝트 수업 기록 관리 사이트(서울 리전)를 연결했다. 계정별 테이블 lesson_workspace_states와 RLS 정책을 만들고, 매직 링크 로그인 UI를 index.html에 추가했다. 실제 로그인 테스트를 위해서는 배포 URL을 Supabase Authentication > URL Configuration의 Site URL/Redirect URLs에 등록해야 한다.

## 2026-08-12 Supabase 및 배포 준비 추가 기록
- Supabase 프로젝트: 수업 기록 관리 사이트 / 서울 리전.
- 매직 링크 로그인 방식을 선택했다. 수업·학생 기록은 로그인한 사용자별로 분리 저장한다.
- Supabase 테이블 `lesson_workspace_states`를 생성했다.
  - `user_id`를 기본 키로 사용한다.
  - RLS를 켰고, 로그인한 사용자는 본인 행만 읽고·추가하고·수정하고·삭제할 수 있다.
- `index.html`에 이메일 로그인 링크 요청 화면과 Supabase 동기화 코드를 추가했다.
- 기존 localStorage 자료가 있고 클라우드 저장 데이터가 비어 있으면, 첫 로그인 시 클라우드로 옮기도록 했다.
- 매직 링크 테스트는 아직 완료하지 않았다. 배포된 HTTPS 주소가 있어야 하며, 배포 후 Supabase Authentication > URL Configuration에 Site URL과 Redirect URL로 등록해야 한다.
- GitHub 저장소: `https://github.com/luna-2025/classinfo.git`.
- GitHub CLI를 설치했지만 GitHub 로그인·커밋·푸시는 아직 진행하지 않았다.
- 사용자는 앞으로 프로그램 설치·업데이트·외부 서비스 변경·배포 전에 반드시 설명과 승인을 먼저 받기를 원한다.