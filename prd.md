# 수업 기록 관리 사이트 PRD

## 목적
코딩 강사가 수업 직후 학생 참여, 이해도, 어려웠던 점, 다음 수업 계획을 빠르게 기록하고 이후 수업에 활용하는 도구다.

## 핵심 사용자 흐름
1. 날짜를 선택한다.
2. 수업을 추가하거나 해당 날짜의 수업을 확인한다.
3. 수업 기록하기를 눌러 수업 전체 기록을 작성한다.
4. 필요한 학생을 추가하여 개인별 피드백을 입력한다.
5. 저장한 기록을 다음 수업 준비에 활용한다.

## 현재 구현된 기능
- 날짜 선택
- 수업 직접 입력 및 추가
- 시작일/종료일 기반 주간 반복 수업
- 수업 목록과 삭제
- 수업 전체 기록
- 학생별 피드백 직접 추가
- 브라우저 로컬 저장
- 기본 반응형 화면

## 다음 우선순위
1. 학생 관리 화면
   - 학생 등록, 과정/반 배정, 학생별 기록 보기
2. 과정 관리 화면
   - 과정/반별 고정 요일, 기간, 학생 명단 관리
3. 엑셀/CSV 학생 명단 가져오기
   - 권장 열: 이름, 학교, 학년, 과정/반
4. 수업 기록 목록과 검색
5. 반복 수업의 특정 날짜만 수정/취소하는 기능
6. 수업 기록에서 실제 과정 학생 명단을 자동으로 불러오기
7. 모바일 UX 다듬기
8. 데이터베이스 연결 및 배포

## 배포와 데이터 원칙
- 사용자는 로그인 없는 사용을 선호한다.
- 모바일과 PC 동기화에는 서버 데이터베이스가 필수다.
- 학생 정보를 다루므로 공개 URL만 사용하는 방식보다 비밀번호/접근 제한을 권장한다.
- 데이터베이스와 배포 구조를 결정한 뒤 로컬 저장 데이터를 이전할 수 있게 설계한다.

## 완료 기준
- 고정 요일 수업과 학생 명단을 과정 단위로 관리할 수 있다.
- 날짜를 선택하면 해당 날짜의 수업이 표시된다.
- 수업 및 학생 피드백이 저장되고 다시 열어도 유지된다.
- PC와 휴대폰에서 같은 데이터를 볼 수 있다.
## Supabase 연결 상태
- 계정별 동기화용 Supabase 테이블과 RLS 정책을 준비했다.
- 이메일 매직 링크 로그인 UI를 구현했다.
- 남은 작업:
  1. GitHub에 현재 파일 커밋·푸시
  2. 정적 웹사이트 배포
  3. Supabase Authentication > URL Configuration에 배포 주소 등록
  4. 본인 이메일로 매직 링크 로그인 테스트
  5. PC와 휴대폰 사이 수업·기록 동기화 확인

## 외부 작업 승인 원칙
프로그램 설치, GitHub 푸시, 배포, Supabase 설정 변경 등 외부에 영향을 주는 작업은 실행 전 사용자에게 먼저 승인을 받는다.