# 작업 안내서

## 프로젝트
- 이름: 수업 기록 관리 사이트
- 작업 폴더: `C:\바이브코딩_코덱스\수업 기록 관리 사이트`
- 현재 실행 파일: `index.html`
- 미리보기: `file:///C:/바이브코딩_코덱스/수업%20기록%20관리%20사이트/index.html`

## 다음 작업 원칙
1. 사용자가 요청한 기능을 우선 구현하고 수정 뒤 미리보기를 자동으로 연다.
2. UI 문구는 한국어로 유지한다.
3. 기존의 차분하고 간결한 카드형 디자인을 유지한다.
4. 현재는 로그인 없는 로컬 저장(localStorage) 방식이다. PC와 휴대폰 동기화 또는 배포 전에는 데이터베이스를 연결해야 한다.
5. 학생 개인정보를 다룰 가능성이 있으므로 공개 배포 전 접근 제한을 사용자와 논의한다.

## 현재 주요 동작
- 날짜 선택 후 수업을 추가한다.
- 반복 수업은 시작일의 요일을 기준으로 종료일까지 매주 표시된다.
- 수업 기록하기에서 전체 수업 기록과 학생별 피드백을 작성한다.
- 학생별 피드백은 학생 추가 버튼으로 직접 추가한다.
- 수업 삭제는 반복 일정 전체를 삭제한다.
- 수업과 기록은 브라우저 localStorage에 저장된다.

## 주의
- 현재 코드가 단일 HTML 파일에 집중되어 있다. 기능이 커질 때는 CSS, JavaScript, 데이터 계층을 분리하는 리팩터링을 우선 검토한다.
- 수정 후 기존 localStorage 데이터가 유지되는지 확인한다.